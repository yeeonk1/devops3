<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cloud.mapper.MemberMapper">
	<insert id="insertMember" parameterType="org.cloud.dto.MemberDto">
		INSERT INTO t_member(user_id, password, user_name, role)
		VALUES (#{userId}, #{password}, #{userName}, 'ROLE_USER')
	</insert>
	
	<select id="selectMemberById" resultType="org.cloud.dto.MemberDto" parameterType="String">
		<![CDATA[
			SELECT user_id, password, user_name, role
			FROM t_member
			WHERE user_id = #{userId}
		]]>
	</select>
</mapper>